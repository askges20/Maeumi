<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hanium.android.maeumi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maeumi">
        <activity android:name=".selftest"></activity>
        <activity android:name=".test_result" />
        <activity android:name=".mainselftest" />
        <activity android:name=".testfirst" />
        <activity android:name=".testclick" /> <!-- 로딩 화면 -->
        <activity android:name=".Activity_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 메인 화면 -->
        <activity android:name=".MainActivity" />
        <activity android:name=".Diary" />
        <activity android:name=".SelfTest" />
        <activity android:name=".ChatBot" />
        <activity android:name=".Board" />
        <activity android:name=".Profile" />
        <activity android:name=".DiaryContent" />
    </application>

</manifest>